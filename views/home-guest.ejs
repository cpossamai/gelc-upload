<%- include('includes/header')%>

<div class="container py-md-5">
<% errors.forEach(function(message){%>
  <div class="alert alert-danger text-center"><%=message%></div>
  <%})%>
  <div class="row align-items-center">
    <div class="col-lg-7 py-3 py-md-5">
      <h1 class="display-3">Help us save paper and trees</h1>      
      <p class="lead text-muted">We are required to keep all your work for a period of time, but this takes a lot of paper, ink, and space in our storage cabinets. Please help us by not printing your work, and upload to our portal instead. We are still keeping your personal information in the lockable cabinets. This portal is secured with the latest security features. By uploading your files, you agree that it is your own work. Once uploaded, anyone will only have read access to your work, but you can upload as many times as you require.</p>
    </div>
    <div class="col-lg-5 pl-lg-5 pb-3 py-lg-5">
        <% regErrors.forEach(function(message){%>
          <div class="alert alert-danger small"><%=message%></div>
          <%})%>
      <form action="/register" method="POST" id="registration-form">

          <div class="form-group">
            <label for="username-register" class="text-muted mb-1"><small>Username</small></label>
            <input name="username" id="username-register" class="form-control" type="text" placeholder="Pick a username">
          </div>
          
          <div class="form-group">
            <label for="email-register" class="text-muted mb-1"><small>Email</small></label>
            <input name="email" id="email-register" class="form-control" type="text" placeholder="you@example.com">
          </div>
          
          <div class="form-group">
            <label for="password-register" class="text-muted mb-1"><small>Password</small></label>
            <input name="password" id="password-register" class="form-control" type="password" placeholder="Create a password">
          </div>
          <input type="hidden" name="_csrf" value="<%=  %>"/>
          
          <button type="submit" class="py-3 mt-4 btn btn-lg btn-success btn-block">Sign up for OurApp</button>
          
        </form>
      </div>
    </div>
  </div>
  
<%- include('includes/footer')%>